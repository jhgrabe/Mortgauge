# Copy to .env and fill in real values. The real .env is gitignored.
# (For now Django falls back to safe dev defaults if these are unset.)

# Django
SECRET_KEY=change-me-generate-a-real-one
DEBUG=true

# Phase 4 (deploy) — switch from SQLite to Postgres via this, not code changes
# DATABASE_URL=postgres://user:password@localhost:5432/homeward
